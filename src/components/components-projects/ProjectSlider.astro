---
import FeaturedProjectCard from "./FeaturedProjectCard.astro";

interface Project {
  title: string;
  category?: string;
  image?: string;
}

interface Props {
  projects: Project[];
}

const { projects } = Astro.props;
---

<div class="relative w-full overflow-hidden group">
  <!-- Navigation Buttons Hint -->
  <div class="absolute left-0 top-0 bottom-0 w-24 bg-linear-to-r from-[#0a0a0a] to-transparent z-10 pointer-events-none opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
  <div class="absolute right-0 top-0 bottom-0 w-24 bg-linear-to-l from-[#0a0a0a] to-transparent z-10 pointer-events-none opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>

  <!-- Slider Container -->
  <div 
    id="project-slider"
    class="flex gap-6 overflow-x-auto pb-8 snap-x snap-mandatory scroll-smooth scrollbar-hide px-4 md:px-[10vw]"
  >
    {projects.map((project) => (
      <FeaturedProjectCard 
        title={project.title} 
        category={project.category} 
        image={project.image} 
      />
    ))}
  </div>

  <!-- Custom Scrollbar/Indicator -->
  <div class="flex justify-center gap-2 mt-4">
    {projects.map((_, i) => (
      <div 
        class="slider-dot w-2 h-2 rounded-full bg-zinc-800 transition-all duration-300"
        data-index={i}
      ></div>
    ))}
  </div>
</div>

<style>
  /* Hide scrollbar for Chrome, Safari and Opera */
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }

  /* Hide scrollbar for IE, Edge and Firefox */
  .scrollbar-hide {
    -ms-overflow-style: none;  /* IE and Edge */
    scrollbar-width: none;  /* Firefox */
  }
</style>

<script>
  const slider = document.getElementById('project-slider');
  const dots = document.querySelectorAll('.slider-dot');

  if (slider) {
    slider.addEventListener('scroll', () => {
      const scrollPos = slider.scrollLeft;
      const cardWidth = slider.querySelector('div')?.clientWidth || 0;
      const gap = 24; // gap-6
      const index = Math.round(scrollPos / (cardWidth + gap));
      
      dots.forEach((dot, i) => {
        if (i === index) {
          dot.classList.add('bg-unicode', 'w-6');
          dot.classList.remove('bg-zinc-800');
        } else {
          dot.classList.remove('bg-unicode', 'w-6');
          dot.classList.add('bg-zinc-800');
        }
      });
    });

    // Initialize first dot
    if (dots[0]) {
      dots[0].classList.add('bg-unicode', 'w-6');
      dots[0].classList.remove('bg-zinc-800');
    }
  }
</script>
